{
  "name": "ghostframe-monorepo",
  "private": false,
  "version": "0.1.0",
  "description": "GhostFrame monorepo",
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo dev",
    "test": "pnpm --filter ghostframe test",
    "lint": "pnpm -r exec tsc --noEmit"
  }
}